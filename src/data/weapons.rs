use bevy::utils::Uuid;

use crate::components::weapons::Weapon;

impl Weapon {
    fn load_all() -> Vec<Weapon> {
        todo!();
    }
    
    fn load(id: Uuid) {
        todo!();
    }

    fn load(name: &str) {
        todo!();
    }
}
